<div class="rounded overflow-hidden shadow-lg mx-2 mb-4">
    <a href="{{ url }}">
        <img class="w-full" src="https://picsum.photos/300/200" alt="">
    </a>

    <div class="px-6 py-4">
        <div class="font-bold text-xl mb-2">{{ title }}</div>
        <p class="text-grey-darker text-base">{{ description }}</p>
    </div>

    <div class="px-6 py-4 flex items-center">
        <span class="text-grey-dark">{{ price }}</span>

        <a href="{{ url }}" class="inline-block bg-green-light rounded-full ml-auto px-8 py-3 text-sm font-semibold no-underline text-black">
            Buy
        </a>
    </div>
</div>
