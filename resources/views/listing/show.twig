{% extends 'layouts.default' %}

{% block content %}

    <div class="flex flex-wrap -mx-4">
        <div class="w-full md:w-1/2">
            <div class="mx-2">
                <h1 class="mt-8 mb-4">{{ listing.title }}</h1>
            
                <p class="mb-4">{{ listing.description }}</p>

                <p class="mb-4">{{ '€ ' ~ (listing.price/100)|number_format(2, ',', '.') }}</p>
            </div>
        </div>

        <div class="w-full md:w-1/2">
            <div class="ml-8 mr-2">
                <h2 class="mt-8">Seller info</h2>
            </div>
        </div>
    </div>

{% endblock %}